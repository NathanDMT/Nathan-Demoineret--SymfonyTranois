{% extends 'base.html.twig' %}

{% block title %}Créer une Galerie{% endblock %}

{% block body %}
    <h1>Créer une Galerie</h1>

    {{ form_start(form) }}
    {{ form_row(form.galleryName) }}
    {{ form_row(form.description) }}
    <button class="btn btn-primary">Créer</button>
    {{ form_end(form) }}
{% endblock %}
